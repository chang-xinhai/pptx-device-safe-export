interface:
  display_name: "PPTX Device-Safe Export"
  short_description: "Flatten PPTX pages while preserving playable media"
  default_prompt: "Use $pptx-device-safe-export to create a cross-device safe PPTX for presenting on another computer."
